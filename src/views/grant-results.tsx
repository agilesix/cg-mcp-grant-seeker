// Skybridge discovers views only under src/views/. Keep this file as a
// behavior-free scanner shim; the host container lives with the host adapter.
import GrantResultsContainer from '../app/hosts/skybridge/views/grant-results.js';

export default GrantResultsContainer;
